{
	"info": {
		"_postman_id": "8c44b931-e129-4d64-9640-bd4465494d45",
		"name": "Track. Enterprise API Collection",
		"description": "API collection for the Track Multi-User Enterprise Task Tracker, backed by SQLite.\n\n**Auth model**: cookie-based sessions (`session_token`, httpOnly). Postman's built-in cookie jar handles this automatically once you run **Login** — no manual header wiring needed. There is no Bearer/API-key auth in this API.\n\n**Roles**: every user is either a regular user or an admin (`isAdmin`). \"Manager\" and \"employee\" are not fixed roles — they're derived from rows in the `manager_employee` table, so any user can be a manager of some people and a report of others. Endpoints under **Admin (User & Hierarchy)** require `isAdmin = true` on the calling session (403 otherwise); endpoints under **Team & Hierarchy** just need an authenticated session (no admin flag), since they only ever return data scoped to the caller; all other endpoints likewise just require an authenticated session (401 otherwise).\n\n**Known gaps, documented on their requests rather than hidden**: bulk task status changes don't trigger *per-task* `task_assigned`/`task_completed` notifications (only single-task updates do) — though bulk operations correctly do trigger the project-wide completion notification if they happen to finish the project. `GET /api/kpi/dashboard` (Analytics & KPIs) is genuinely org-wide by design — every project counts for every caller — but that means its `memberWorkloadList`/`projectHealthList` sections expose every project's name and every person's task/time totals to every employee, not just people you share a project with. Neither is a crash risk — see each request's description for specifics.\n\n**Self-signup is locked down**: `POST /api/auth/signup` only succeeds while the system has zero users (it creates the first account as admin). After that it always returns 403 — new accounts must be created via **Provision User (Admin)**.\n\n**Suggested run order** (this collection is wired for it via test scripts that auto-populate collection variables like `{{projectId}}`, `{{taskId}}`, `{{managerUserId}}`, `{{employeeUserId}}`, `{{templateId}}` — no manual copy-pasting of IDs required):\n1. Authentication → Sign Up (only if the DB is empty) → Login\n2. Admin → provision a manager + an employee, link them\n3. Team & Hierarchy → confirm the link (Get My Direct Reports / Get My Managers), try the rollups\n4. Projects → create a project (as the logged-in admin/manager)\n5. Tasks → create/update/bulk-update a task, assigning it to the employee\n6. Notifications → run **Get My Notifications** as the employee to see the auto-generated `task_assigned` notification from step 5\n7. Analytics & KPIs → check the dashboard as both users to see the org-wide-labeling gap firsthand\n8. Templates → create a template, spin up a project from it\n\nWithin each folder, non-destructive requests are ordered first; **Delete**/**Unassign** requests are last and destructive — run them only once you're done exploring that folder.",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
	},
	"item": [
		{
			"name": "Authentication",
			"item": [
				{
					"name": "Sign Up (Bootstrap First Admin)",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    pm.test('Bootstrap admin created', function () {",
									"        pm.expect(pm.response.code).to.equal(201);",
									"    });",
									"} else if (pm.response.code === 403) {",
									"    console.log('System already has users — self-signup is disabled by design. Use Login instead.');",
									"} else {",
									"    pm.test('Unexpected signup response', function () {",
									"        pm.expect.fail('Signup returned ' + pm.response.code + ': ' + pm.response.text());",
									"    });",
									"}"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"AdminPass123!\",\n    \"email\": \"admin@company.local\",\n    \"displayName\": \"System Administrator\",\n    \"jobTitle\": \"Administrator\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/auth/signup",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"auth",
								"signup"
							]
						},
						"description": "Registers a new account **only if the `users` table is currently empty** — that account is automatically flagged `isAdmin = true`. Once any user exists, this always returns `403 {\"error\":\"Self-signup is disabled. New user accounts must be provisioned by an administrator.\"}`. There is no way to re-enable open signup short of an empty database; use **Provision User (Admin)** for every account after the first.\n\n**Responses**\n- `201` — `{ \"message\": \"System administrator registered successfully!\" }`\n- `400` — missing `username`/`password`, or username already taken\n- `403` — system already initialized (expected on any run after the first)"
					},
					"response": [
						{
							"name": "201 Created — bootstrap admin",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"AdminPass123!\",\n    \"email\": \"admin@company.local\",\n    \"displayName\": \"System Administrator\",\n    \"jobTitle\": \"Administrator\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/auth/signup",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "signup"]
								}
							},
							"status": "Created",
							"code": 201,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"message\": \"System administrator registered successfully!\"\n}"
						},
						{
							"name": "403 Forbidden — signup already disabled",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"anyone\",\n    \"password\": \"whatever123\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/auth/signup",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "signup"]
								}
							},
							"status": "Forbidden",
							"code": 403,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Self-signup is disabled. New user accounts must be provisioned by an administrator.\"\n}"
						}
					]
				},
				{
					"name": "Login (Sign In)",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"pm.test('Login succeeded and returned a user profile', function () {",
									"    pm.response.to.have.status(200);",
									"    const jsonData = pm.response.json();",
									"    pm.expect(jsonData.user).to.have.property('id');",
									"});",
									"",
									"pm.test('Session cookie was set', function () {",
									"    pm.expect(pm.cookies.has('session_token')).to.be.true;",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"AdminPass123!\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/auth/login",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"auth",
								"login"
							]
						},
						"description": "Verifies credentials, writes a hashed session row to SQLite (7-day expiry), and sets an `httpOnly`, `SameSite=Strict` `session_token` cookie. Postman's cookie jar stores this automatically for `{{baseUrl}}` — every subsequent request in this collection reuses it without any manual setup.\n\n**Responses**\n- `200` — `{ \"message\": \"Login successful!\", \"user\": { \"id\", \"username\", \"email\", \"displayName\", \"jobTitle\", \"isAdmin\" } }`\n- `400` — missing `username`/`password`\n- `401` — invalid username or password"
					},
					"response": [
						{
							"name": "200 OK — logged in",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"AdminPass123!\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/auth/login",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "login"]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" },
								{ "key": "Set-Cookie", "value": "session_token=<hex>; Path=/; HttpOnly; SameSite=Strict" }
							],
							"cookie": [],
							"body": "{\n    \"message\": \"Login successful!\",\n    \"user\": {\n        \"id\": \"1294e8df-18ed-402a-8080-f94be8679b6b\",\n        \"username\": \"admin\",\n        \"email\": \"admin@company.local\",\n        \"displayName\": \"System Administrator\",\n        \"jobTitle\": \"Administrator\",\n        \"isAdmin\": true\n    }\n}"
						},
						{
							"name": "401 Unauthorized — bad credentials",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"WrongPassword\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/auth/login",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "login"]
								}
							},
							"status": "Unauthorized",
							"code": 401,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Invalid username or password.\"\n}"
						}
					]
				},
				{
					"name": "Get Current User (Me)",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/auth/me",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"auth",
								"me"
							]
						},
						"description": "Returns the profile bound to the active session cookie. Useful as a quick \"am I still logged in / am I admin\" check before running admin-only requests.\n\n**Responses**\n- `200` — `{ \"user\": { \"id\", \"username\", \"email\", \"displayName\", \"jobTitle\", \"isAdmin\" } }`\n- `401` — no session cookie, or session missing/expired in the database"
					},
					"response": [
						{
							"name": "200 OK",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/auth/me",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "me"]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"user\": {\n        \"id\": \"1294e8df-18ed-402a-8080-f94be8679b6b\",\n        \"username\": \"admin\",\n        \"email\": \"admin@company.local\",\n        \"displayName\": \"System Administrator\",\n        \"jobTitle\": \"Administrator\",\n        \"isAdmin\": true\n    }\n}"
						},
						{
							"name": "401 Unauthorized — no/expired session",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/auth/me",
									"host": ["{{baseUrl}}"],
									"path": ["api", "auth", "me"]
								}
							},
							"status": "Unauthorized",
							"code": 401,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Unauthorized. Session not found.\"\n}"
						}
					]
				},
				{
					"name": "Logout",
					"request": {
						"method": "POST",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/auth/logout",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"auth",
								"logout"
							]
						},
						"description": "Deletes the session row from SQLite and clears the `session_token` cookie. Idempotent — safe to call even with no active session.\n\n**Responses**\n- `200` — `{ \"message\": \"Logged out successfully.\" }` (always, even if there was nothing to log out of)"
					},
					"response": []
				}
			],
			"description": "Session lifecycle: bootstrap the first admin, sign in, inspect the current session, sign out. Run **Login** before anything else in this collection — every other folder depends on its cookie."
		},
		{
			"name": "Admin (User & Hierarchy)",
			"item": [
				{
					"name": "Get All Users (Admin)",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/admin/users",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"users"
							]
						},
						"description": "Lists every user in the org with their derived `managerIds` (who they report to) and `employeeIds` (who reports to them) — computed from the `manager_employee` table, not stored on the user row. **Admin only.**\n\n**Responses**\n- `200` — array of `{ id, username, email, displayName, jobTitle, isAdmin, isActive, createdAt, managerIds: [], employeeIds: [] }`\n- `401` — not logged in\n- `403` — logged in but not an admin"
					},
					"response": [
						{
							"name": "200 OK",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/admin/users",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "users"]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "[\n    {\n        \"id\": \"1294e8df-18ed-402a-8080-f94be8679b6b\",\n        \"username\": \"admin\",\n        \"email\": \"admin@company.local\",\n        \"displayName\": \"System Administrator\",\n        \"jobTitle\": \"Administrator\",\n        \"isAdmin\": true,\n        \"isActive\": true,\n        \"createdAt\": \"2026-08-27T15:00:00.000Z\",\n        \"managerIds\": [],\n        \"employeeIds\": [\"5a11...employee\"]\n    }\n]"
						},
						{
							"name": "403 Forbidden — not an admin",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/admin/users",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "users"]
								}
							},
							"status": "Forbidden",
							"code": 403,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Access denied. Admin privileges required.\"\n}"
						}
					]
				},
				{
					"name": "Provision Manager Account (Admin)",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    const jsonData = pm.response.json();",
									"    pm.collectionVariables.set('managerUserId', jsonData.userId);",
									"    console.log('managerUserId set to', jsonData.userId);",
									"}",
									"pm.test('Manager provisioned', function () {",
									"    pm.response.to.have.status(201);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"manager1\",\n    \"password\": \"Password123!\",\n    \"email\": \"manager1@company.local\",\n    \"displayName\": \"Morgan Manager\",\n    \"jobTitle\": \"Engineering Manager\",\n    \"isAdmin\": false,\n    \"managerIds\": []\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/admin/users",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"users"
							]
						},
						"description": "Creates a user account directly — this is the only supported way to add people once the system has been bootstrapped (self-signup is disabled). Sets `managerUserId` from the response for later requests. \"Manager\" isn't a role flag here; this account becomes a manager the moment someone reports to it (see **Assign Manager**). **Admin only.**\n\n**Responses**\n- `201` — `{ \"message\": \"User provisioned successfully.\", \"userId\" }`\n- `400` — missing `username`/`password`, or username already taken\n- `403` — caller is not an admin"
					},
					"response": [
						{
							"name": "201 Created",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"manager1\",\n    \"password\": \"Password123!\",\n    \"jobTitle\": \"Engineering Manager\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/admin/users",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "users"]
								}
							},
							"status": "Created",
							"code": 201,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"message\": \"User provisioned successfully.\",\n    \"userId\": \"a3f1c2e4-9b8d-4a7e-8c1f-2d3e4f5a6b7c\"\n}"
						},
						{
							"name": "400 Bad Request — username taken",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"Password123!\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/admin/users",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "users"]
								}
							},
							"status": "Bad Request",
							"code": 400,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Username already exists.\"\n}"
						}
					]
				},
				{
					"name": "Provision Employee Account (Admin)",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    const jsonData = pm.response.json();",
									"    pm.collectionVariables.set('employeeUserId', jsonData.userId);",
									"    console.log('employeeUserId set to', jsonData.userId);",
									"}",
									"pm.test('Employee provisioned', function () {",
									"    pm.response.to.have.status(201);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"employee1\",\n    \"password\": \"Password123!\",\n    \"email\": \"employee1@company.local\",\n    \"displayName\": \"Erin Employee\",\n    \"jobTitle\": \"Frontend Engineer\",\n    \"isAdmin\": false,\n    \"managerIds\": []\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/admin/users",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"users"
							]
						},
						"description": "Same endpoint as **Provision Manager Account** — creates the employee standalone (no manager yet) so the next request can demonstrate **Assign Manager** explicitly. You could equally pass `managerIds: [\"{{managerUserId}}\"]` here to link at creation time; both paths run the same cycle-detection check. **Admin only.**"
					},
					"response": []
				},
				{
					"name": "Update User (Admin)",
					"request": {
						"method": "PUT",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"displayName\": \"Erin Employee (Senior)\",\n    \"jobTitle\": \"Senior Frontend Engineer\",\n    \"isActive\": true\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/admin/users/:id",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"users",
								":id"
							],
							"variable": [
								{
									"key": "id",
									"value": "{{employeeUserId}}"
								}
							]
						},
						"description": "Partially updates a user's profile — every field is optional and only supplied fields are changed (`COALESCE` under the hood). **Important**: if you include `managerIds` in the body, it **fully replaces** that user's manager set (existing links not in the list are deleted first) — omit it entirely to leave manager relationships untouched, as this example does. Setting `isActive: false` immediately blocks that user from authenticating (checked in the login and session-lookup queries). **Admin only.**\n\n**Responses**\n- `200` — `{ \"message\": \"User updated successfully.\" }`\n- `404` — user ID not found\n- `403` — caller is not an admin"
					},
					"response": []
				},
				{
					"name": "Assign Manager (Admin)",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"pm.test('Manager link created (or already existed)', function () {",
									"    pm.expect([200, 201]).to.include(pm.response.code);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"managerId\": \"{{managerUserId}}\",\n    \"employeeId\": \"{{employeeUserId}}\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/admin/managers",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"managers"
							]
						},
						"description": "Links `managerId` as a manager of `employeeId`. An employee can have multiple managers — this just adds another row, it never replaces existing links. A breadth-first cycle check walks the existing hierarchy before inserting, so a manager can never end up (directly or transitively) reporting to their own report. Re-running this with the same pair is a harmless no-op (`INSERT OR IGNORE`). **Admin only.**\n\n**Responses**\n- `201` — `{ \"message\": \"Manager assigned successfully.\" }`\n- `400` — missing `managerId`/`employeeId`, or the pair would create a cycle\n- `403` — caller is not an admin"
					},
					"response": [
						{
							"name": "201 Created",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"managerId\": \"a3f1c2e4-9b8d-4a7e-8c1f-2d3e4f5a6b7c\",\n    \"employeeId\": \"c8d2b3f5-0a9e-4b8f-9d2e-3f4a5b6c7d8e\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/admin/managers",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "managers"]
								}
							},
							"status": "Created",
							"code": 201,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"message\": \"Manager assigned successfully.\"\n}"
						},
						{
							"name": "400 Bad Request — would create a hierarchy loop",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"managerId\": \"c8d2b3f5-0a9e-4b8f-9d2e-3f4a5b6c7d8e\",\n    \"employeeId\": \"a3f1c2e4-9b8d-4a7e-8c1f-2d3e4f5a6b7c\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/admin/managers",
									"host": ["{{baseUrl}}"],
									"path": ["api", "admin", "managers"]
								}
							},
							"status": "Bad Request",
							"code": 400,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Cannot assign manager: relationship would create a management hierarchy loop.\"\n}"
						}
					]
				},
				{
					"name": "Unassign Manager (Admin)",
					"request": {
						"method": "DELETE",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"managerId\": \"{{managerUserId}}\",\n    \"employeeId\": \"{{employeeUserId}}\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/admin/managers",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"admin",
								"managers"
							]
						},
						"description": "Removes one manager-employee link. Idempotent — returns `200` even if the pair wasn't linked. **Destructive to the demo hierarchy — run this last**, since later requests in the Tasks folder rely on the manager/employee link to demonstrate assignee validation. **Admin only.**\n\n**Responses**\n- `200` — `{ \"message\": \"Manager unassigned successfully.\" }` (always)\n- `403` — caller is not an admin"
					},
					"response": []
				}
			],
			"description": "Admin-only endpoints (403 for any non-admin session) for provisioning accounts and shaping the manager/employee hierarchy. Run **Provision Manager Account** and **Provision Employee Account** before **Assign Manager** — both earlier requests auto-populate `{{managerUserId}}`/`{{employeeUserId}}` via their test scripts."
		},
		{
			"name": "Team & Hierarchy",
			"item": [
				{
					"name": "Get My Direct Reports (Team)",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/users/me/team",
							"host": ["{{baseUrl}}"],
							"path": ["api", "users", "me", "team"]
						},
						"description": "Returns the people who report **directly** to the logged-in user, derived live from `manager_employee` (there's no stored \"is a manager\" flag — you're a manager purely because this list is non-empty). Used by the frontend to compute `isManager`.\n\n**Responses**\n- `200` — array of `{ id, username, email, displayName, jobTitle, isAdmin }` (empty array if you manage no one)"
					},
					"response": []
				},
				{
					"name": "Get My Managers",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/users/me/managers",
							"host": ["{{baseUrl}}"],
							"path": ["api", "users", "me", "managers"]
						},
						"description": "Returns everyone who directly manages the logged-in user. Can be more than one — an employee having multiple managers is a supported case, not an edge case.\n\n**Responses**\n- `200` — array of `{ id, username, email, displayName, jobTitle, isAdmin }` (empty array if you have no manager)"
					},
					"response": []
				},
				{
					"name": "Get All Reports (Recursive Skip-Level)",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/users/me/team/all",
							"host": ["{{baseUrl}}"],
							"path": ["api", "users", "me", "team", "all"]
						},
						"description": "Like **Get My Direct Reports**, but walks the full hierarchy with a `WITH RECURSIVE` query — direct reports, their reports, and so on. Backs the **Team Directory** frontend view. De-duplicated even if someone is reachable through more than one management chain.\n\n**Responses**\n- `200` — array of `{ id, username, email, displayName, jobTitle, isAdmin }`"
					},
					"response": []
				},
				{
					"name": "Get Eligible Assignees for Task",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/users/me/assignees?projectId={{projectId}}",
							"host": ["{{baseUrl}}"],
							"path": ["api", "users", "me", "assignees"],
							"query": [
								{ "key": "projectId", "value": "{{projectId}}", "description": "Optional. Include to also pull in this project's members as candidates." }
							]
						},
						"description": "Populates the assignee dropdown shown in the task create/edit modal: always includes yourself and your direct reports; if `projectId` is given, also includes that project's members (de-duplicated). This is deliberately the same rule set `isValidAssignee` enforces server-side on task create/update, so the dropdown never offers a choice the backend would reject.\n\nOmit `projectId` (or drop the query string entirely) to get just yourself + direct reports, useful outside a project context.\n\n**Responses**\n- `200` — array of `{ id, username, email, displayName, jobTitle, isAdmin }`\n- `404` — a `projectId` was given but you don't own it and aren't a member (same check task creation uses)"
					},
					"response": [
						{
							"name": "404 Not Found — no access to that project",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/users/me/assignees?projectId=00000000-0000-0000-0000-000000000000",
									"host": ["{{baseUrl}}"],
									"path": ["api", "users", "me", "assignees"],
									"query": [{ "key": "projectId", "value": "00000000-0000-0000-0000-000000000000" }]
								}
							},
							"status": "Not Found",
							"code": 404,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Project not found or unauthorized.\"\n}"
						}
					]
				},
				{
					"name": "Get Manager Performance Rollup",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/reports/manager",
							"host": ["{{baseUrl}}"],
							"path": ["api", "reports", "manager"]
						},
						"description": "Skip-level performance rollup across every direct and indirect report (via `getRecursiveReports`) — a summary block plus one stats entry per report (task counts by status, completion rate, overdue count, total time logged, and that report's own list of managers).\n\n**⚠️ Known scoping gap (documented, not fixed as of this API version)**: per-report stats are computed with `SELECT ... FROM tasks WHERE assignee_id = ?` and are **not** filtered by project visibility — they include every task ever assigned to that person company-wide, even ones created by a completely unrelated manager in a project you have no access to. Verified: a manager whose only relationship to an employee is via `manager_employee` will see that employee's *combined* task/time totals from every manager who has ever assigned them anything, not just their own. Contrast with **Get Project Time Rollup** below, which *is* correctly scoped to one project's visibility. Treat the numbers here as \"this person's total workload across the company,\" not \"work I personally assigned or can see.\"\n\n**Responses**\n- `200` — `{ summary: { totalReports, teamTotalTasks, teamCompletedTasks, teamCompletionRate, teamOverdueTasks, teamTotalTimeLogged }, reports: [{ user, managers: [...], stats: { totalTasks, completedTasks, inProgressTasks, todoTasks, overdueTasks, completionRate, totalTimeLogged } }] }` (empty `reports` array if you manage no one)"
					},
					"response": []
				},
				{
					"name": "Get Company User Directory",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/users/all",
							"host": ["{{baseUrl}}"],
							"path": ["api", "users", "all"]
						},
						"description": "Lists every active user in the company (id, username, displayName, jobTitle, email) — used to populate the \"add teammate to project\" candidate picker. **Any authenticated user can call this**, not just admins or managers; there's no scoping to the caller's own reports or existing projects. Reasonable for an internal tool's people-picker, but worth confirming that company-wide visibility (including everyone's email) is the intended scope.\n\n**Responses**\n- `200` — array of `{ id, username, displayName, jobTitle, email }`"
					},
					"response": []
				}
			],
			"description": "Read-only endpoints exposing the manager/employee hierarchy and cross-user directory data — everything here just requires an authenticated session (no admin flag needed, unlike the **Admin** folder). Run after **Assign Manager** so the recursive/rollup endpoints have real hierarchy data to show."
		},
		{
			"name": "Projects",
			"item": [
				{
					"name": "Get All Projects",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/projects",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects"
							]
						},
						"description": "Returns every project the current user can see: projects they own, plus projects where they appear in `project_members`. Each project includes its full `tasks` array and a `members` array. **Note**: the `members` entries come straight from the database and use snake_case field names (`display_name`, `job_title`, `joined_at`), unlike every other camelCase response in this API — that's a real quirk of the current implementation, not a typo in this doc.\n\n**Responses**\n- `200` — array of `{ id, name, description, url, github, ownerId, createdAt, members: [{ id, username, display_name, job_title, joined_at }], tasks: [...] }`\n- `401` — not logged in"
					},
					"response": []
				},
				{
					"name": "Create Project",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    const jsonData = pm.response.json();",
									"    pm.collectionVariables.set('projectId', jsonData.id);",
									"    console.log('projectId set to', jsonData.id);",
									"}",
									"pm.test('Project created', function () {",
									"    pm.response.to.have.status(201);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"name\": \"Enterprise Portal\",\n    \"description\": \"Internal management system\",\n    \"url\": \"https://portal.company.local\",\n    \"github\": \"https://github.com/company/portal\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects"
							]
						},
						"description": "Creates a project owned by the logged-in user and automatically adds them as a `project_members` row (so they show up as a member on subsequent `GET`s). Sets `{{projectId}}` from the response for use in the Tasks and later Projects requests. **Quirk to note**: the creation response itself always reports `\"members\": []` even though the owner membership row was just inserted — a follow-up `GET /api/projects` will correctly show it. Only `name` is required.\n\n**Responses**\n- `201` — the new project (see shape above; `tasks` and `members` empty in this response)\n- `400` — missing `name`"
					},
					"response": [
						{
							"name": "201 Created",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"name\": \"Enterprise Portal\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects"]
								}
							},
							"status": "Created",
							"code": 201,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"id\": \"b334d8fb-41a1-423e-aa0c-26ad710887b5\",\n    \"name\": \"Enterprise Portal\",\n    \"description\": \"\",\n    \"url\": \"\",\n    \"github\": \"\",\n    \"ownerId\": \"1294e8df-18ed-402a-8080-f94be8679b6b\",\n    \"createdAt\": \"2026-08-27T15:51:52.135Z\",\n    \"members\": [],\n    \"tasks\": []\n}"
						}
					]
				},
				{
					"name": "Update Project Details",
					"request": {
						"method": "PUT",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"name\": \"Enterprise Portal V2\",\n    \"description\": \"Internal management system with OAuth2\",\n    \"url\": \"https://portal.company.local\",\n    \"github\": \"https://github.com/company/portal\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/:id",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":id"
							],
							"variable": [
								{
									"key": "id",
									"value": "{{projectId}}"
								}
							]
						},
						"description": "Updates project metadata. **Owner only** — a project member who isn't the owner gets `403`, even though members can freely add/edit tasks inside the project (see Tasks folder).\n\n**Responses**\n- `200` — the updated project, with live `tasks`/`members` arrays\n- `400` — missing `name`\n- `403` — logged-in user is a member but not the owner\n- `404` — project doesn't exist"
					},
					"response": [
						{
							"name": "403 Forbidden — not the owner",
							"originalRequest": {
								"method": "PUT",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"name\": \"Renamed by a non-owner\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:id",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":id"],
									"variable": [{ "key": "id", "value": "{{projectId}}" }]
								}
							},
							"status": "Forbidden",
							"code": 403,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Forbidden. Only the project owner can edit project details.\"\n}"
						}
					]
				},
				{
					"name": "Add Project Member (Owner)",
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"userId\": \"{{employeeUserId}}\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/:id/members",
							"host": ["{{baseUrl}}"],
							"path": ["api", "projects", ":id", "members"],
							"variable": [
								{ "key": "id", "value": "{{projectId}}" }
							]
						},
						"description": "Adds an active user as a member of the project — from then on they get full board visibility (every task, not just ones assigned to them) per the collaborative-project design. **Owner only.**\n\n**⚠️ No hierarchy check**: unlike task-assignment (`isValidAssignee`, which restricts to self/member/direct-report), this endpoint lets an owner add **any** active user in the company, not just their own direct reports — despite \"manager adds their own report\" being the original design intent. Confirmed: an owner with zero management relationship to a user can still add them. Worth knowing if you're relying on org-hierarchy as an access boundary.\n\n**Responses**\n- `201` — `{ message, members: [{ id, username, displayName, jobTitle, email, joinedAt }] }` (full updated member list)\n- `400` — missing `userId`\n- `403` — caller is not the project owner\n- `404` — project doesn't exist, or `userId` doesn't match an active user"
					},
					"response": [
						{
							"name": "403 Forbidden — not the owner",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"userId\": \"{{employeeUserId}}\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:id/members",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":id", "members"],
									"variable": [{ "key": "id", "value": "{{projectId}}" }]
								}
							},
							"status": "Forbidden",
							"code": 403,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Forbidden. Only the project owner can add project members.\"\n}"
						}
					]
				},
				{
					"name": "Remove Project Member (Owner)",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/projects/:id/members/:userId",
							"host": ["{{baseUrl}}"],
							"path": ["api", "projects", ":id", "members", ":userId"],
							"variable": [
								{ "key": "id", "value": "{{projectId}}" },
								{ "key": "userId", "value": "{{employeeUserId}}" }
							]
						},
						"description": "Revokes a member's access to the project (they immediately lose the full-board visibility membership granted). **Owner only**, and the owner is explicitly protected from removing themselves — there's no way to leave your own project via this endpoint (delete the project instead if that's the goal).\n\n**Responses**\n- `200` — `{ message, members: [...] }` (full updated member list, always returned even if the given `userId` wasn't a member)\n- `400` — attempted to remove the project owner\n- `403` — caller is not the project owner\n- `404` — project doesn't exist"
					},
					"response": [
						{
							"name": "400 Bad Request — cannot remove the owner",
							"originalRequest": {
								"method": "DELETE",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/projects/:id/members/:userId",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":id", "members", ":userId"],
									"variable": [
										{ "key": "id", "value": "{{projectId}}" },
										{ "key": "userId", "value": "OWNER_USER_ID" }
									]
								}
							},
							"status": "Bad Request",
							"code": 400,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Project owner cannot be removed from project members.\"\n}"
						}
					]
				},
				{
					"name": "Get Project Time Rollup",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/projects/:id/time-rollup",
							"host": ["{{baseUrl}}"],
							"path": ["api", "projects", ":id", "time-rollup"],
							"variable": [
								{ "key": "id", "value": "{{projectId}}" }
							]
						},
						"description": "Aggregates logged time for this **one project only** — correctly scoped by `WHERE project_id = ?` throughout (contrast with **Get Manager Performance Rollup** in the Team & Hierarchy folder, which is not project-scoped). Requires project ownership or membership.\n\nReturns four views of the same underlying `time_sessions`/`tasks` data:\n- `totalProjectTimeSeconds` — sum of `time_logged` across every task in the project\n- `memberRollup` — total time + task count per assignee, sorted highest first\n- `taskRollup` — top 20 tasks by time logged (only tasks with `time_logged > 0`)\n- `sessions` — the 50 most recent individual timer sessions, newest first\n\n**Responses**\n- `200` — `{ projectId, projectName, totalProjectTimeSeconds, memberRollup: [...], taskRollup: [...], sessions: [...] }`\n- `404` — project doesn't exist, or caller isn't the owner/a member"
					},
					"response": []
				},
				{
					"name": "Delete Project Workspace",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/projects/:id",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":id"
							],
							"variable": [
								{
									"key": "id",
									"value": "{{projectId}}"
								}
							]
						},
						"description": "Deletes the project and cascades to every task, task assignment, time session, and membership row underneath it (both via SQLite `ON DELETE CASCADE` and explicit cleanup queries as a belt-and-suspenders measure). **Owner only** — an outsider or non-owner member gets `404`, and this action cannot be undone. **Run this last** — Tasks, Templates, and the member/time-rollup requests above all reuse `{{projectId}}`.\n\n**Responses**\n- `200` — `{ \"message\": \"Project deleted successfully.\" }`\n- `404` — project doesn't exist, or caller isn't the owner"
					},
					"response": []
				}
			],
			"description": "Project CRUD, scoped to whatever the logged-in user owns or is a member of, plus owner-only membership management and a project-scoped time-tracking rollup. Editing/deleting metadata and managing members is owner-only; adding tasks is open to any project member (see Tasks folder)."
		},
		{
			"name": "Tasks",
			"item": [
				{
					"name": "Create Task in Project",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    const jsonData = pm.response.json();",
									"    pm.collectionVariables.set('taskId', jsonData.id);",
									"    console.log('taskId set to', jsonData.id);",
									"}",
									"pm.test('Task created', function () {",
									"    pm.response.to.have.status(201);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"title\": \"Implement User Authentication\",\n    \"description\": \"Create login and signup endpoints with SQLite storage.\",\n    \"status\": \"todo\",\n    \"priority\": \"high\",\n    \"scheduleDate\": \"2026-09-01T09:00\",\n    \"deadline\": \"2026-09-15T17:00\",\n    \"reminder\": true,\n    \"urgent\": true,\n    \"important\": true,\n    \"assigneeId\": \"{{employeeUserId}}\",\n    \"subtasks\": [\n        { \"id\": \"s1\", \"text\": \"Setup SQLite tables\", \"completed\": true },\n        { \"id\": \"s2\", \"text\": \"Add session cookies\", \"completed\": true }\n    ],\n    \"timeLogged\": 0,\n    \"timeSessions\": [],\n    \"timerStarted\": null,\n    \"pomodoroSessions\": []\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/:projectId/tasks",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":projectId",
								"tasks"
							],
							"variable": [
								{
									"key": "projectId",
									"value": "{{projectId}}"
								}
							]
						},
						"description": "Creates a task in the project. Requires the caller to own the project or be a member. `assigneeId` is optional — omit it to self-assign — but if provided, it's validated server-side: it must be an active user who is either the caller themself, a member of this project, or a **direct** report of the caller. This example demonstrates the manager→direct-report case, so **Assign Manager** must have run first for it to pass.\n\n**Responses**\n- `201` — the new task, `assigneeId`/`createdById` reflecting who it's for and who made it\n- `400` — missing `title`, or `assigneeId` fails the checks above\n- `404` — project doesn't exist or caller has no access to it"
					},
					"response": [
						{
							"name": "400 Bad Request — invalid assignee",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"title\": \"Task for a stranger\",\n    \"assigneeId\": \"00000000-0000-0000-0000-000000000000\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:projectId/tasks",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":projectId", "tasks"],
									"variable": [{ "key": "projectId", "value": "{{projectId}}" }]
								}
							},
							"status": "Bad Request",
							"code": 400,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Invalid assigneeId. Assignee must be an active user who is a project member or your direct report.\"\n}"
						}
					]
				},
				{
					"name": "Update Task Details",
					"request": {
						"method": "PUT",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"title\": \"Implement User Authentication\",\n    \"description\": \"Create login and signup endpoints with SQLite storage.\",\n    \"status\": \"done\",\n    \"priority\": \"high\",\n    \"scheduleDate\": \"2026-09-01T09:00\",\n    \"deadline\": \"2026-09-15T17:00\",\n    \"reminder\": true,\n    \"urgent\": true,\n    \"important\": true,\n    \"timeLogged\": 3600,\n    \"timeSessions\": [\n        { \"id\": \"sess1\", \"startTime\": \"2026-09-01T09:00:00Z\", \"endTime\": \"2026-09-01T10:00:00Z\", \"duration\": 3600 }\n    ]\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/:projectId/tasks/:taskId",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":projectId",
								"tasks",
								":taskId"
							],
							"variable": [
								{
									"key": "projectId",
									"value": "{{projectId}}"
								},
								{
									"key": "taskId",
									"value": "{{taskId}}"
								}
							]
						},
						"description": "Full update of a task's fields. Requires project ownership/membership (checked before the task lookup). Setting `status` to `\"done\"` stamps `completedAt`; moving it back off `\"done\"` clears it. `timeSessions` entries are upserted into the normalized `time_sessions` table (used for the time-tracking rollups) in addition to updating the task's own `timeLogged` cache. Re-supplying `assigneeId` with a *different* value re-runs the same validation as task creation; leaving it unchanged skips re-validation.\n\n**Responses**\n- `200` — the updated task\n- `400` — missing `title`, or a changed `assigneeId` fails validation\n- `404` — project not found/unauthorized, or task doesn't exist in this project"
					},
					"response": []
				},
				{
					"name": "Bulk Task Operations",
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"taskIds\": [\"{{taskId}}\"],\n    \"action\": \"status\",\n    \"value\": \"in-progress\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/:projectId/tasks/bulk",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":projectId",
								"tasks",
								"bulk"
							],
							"variable": [
								{
									"key": "projectId",
									"value": "{{projectId}}"
								}
							]
						},
						"description": "One endpoint, three `action` values — pick one per call by editing the body:\n- `\"action\": \"status\", \"value\": \"todo\" | \"in-progress\" | \"done\"` — bulk status change (stamps/clears `completedAt` per task, same as a single update)\n- `\"action\": \"priority\", \"value\": \"low\" | \"medium\" | \"high\"` — bulk priority change\n- `\"action\": \"delete\"` (`value` ignored) — bulk permanent delete\n\nSee the saved example responses on this request for the priority and delete variants. Requires project ownership/membership; `taskIds` outside the given `projectId` are silently skipped rather than erroring.\n\n**Responses**\n- `200` — the project's full remaining task list after the operation\n- `400` — empty/missing `taskIds`, or missing `action`\n- `404` — project not found or caller has no access to it"
					},
					"response": [
						{
							"name": "200 OK — priority variant",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"taskIds\": [\"{{taskId}}\"],\n    \"action\": \"priority\",\n    \"value\": \"low\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:projectId/tasks/bulk",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":projectId", "tasks", "bulk"],
									"variable": [{ "key": "projectId", "value": "{{projectId}}" }]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "[\n    {\n        \"id\": \"0e48f7a5-99c2-463e-ae9a-dad00a2617ed\",\n        \"title\": \"Implement User Authentication\",\n        \"priority\": \"low\",\n        \"status\": \"in-progress\"\n    }\n]"
						},
						{
							"name": "200 OK — delete variant (use with care)",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"taskIds\": [\"{{taskId}}\"],\n    \"action\": \"delete\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:projectId/tasks/bulk",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":projectId", "tasks", "bulk"],
									"variable": [{ "key": "projectId", "value": "{{projectId}}" }]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "[]"
						},
						{
							"name": "404 Not Found — no access to project",
							"originalRequest": {
								"method": "POST",
								"header": [
									{ "key": "Content-Type", "value": "application/json" }
								],
								"body": {
									"mode": "raw",
									"raw": "{\n    \"taskIds\": [\"{{taskId}}\"],\n    \"action\": \"status\",\n    \"value\": \"done\"\n}"
								},
								"url": {
									"raw": "{{baseUrl}}/api/projects/:projectId/tasks/bulk",
									"host": ["{{baseUrl}}"],
									"path": ["api", "projects", ":projectId", "tasks", "bulk"],
									"variable": [{ "key": "projectId", "value": "{{projectId}}" }]
								}
							},
							"status": "Not Found",
							"code": 404,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"error\": \"Project not found or unauthorized.\"\n}"
						}
					]
				},
				{
					"name": "Delete Task",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/projects/:projectId/tasks/:taskId",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								":projectId",
								"tasks",
								":taskId"
							],
							"variable": [
								{
									"key": "projectId",
									"value": "{{projectId}}"
								},
								{
									"key": "taskId",
									"value": "{{taskId}}"
								}
							]
						},
						"description": "Permanently deletes a single task (and its `time_sessions` rows, via cascade). Requires project ownership/membership. **Run this last** in the folder — it consumes `{{taskId}}`.\n\n**Responses**\n- `200` — `{ \"message\": \"Task deleted successfully.\" }`\n- `404` — project not found/unauthorized, or task doesn't exist"
					},
					"response": []
				}
			],
			"description": "Task CRUD scoped to project access — every request here now requires the caller to own the project or be a member of it (previously only task *creation* enforced this; `PUT`/`DELETE`/bulk are equally protected). Chained via `{{projectId}}`/`{{taskId}}` from the Projects folder."
		},
		{
			"name": "Templates",
			"item": [
				{
					"name": "Get Reusable Templates",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/templates",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"templates"
							]
						},
						"description": "Lists the current user's own saved templates (owner-scoped, not shared across users). Does **not** include the two built-in templates (`onboarding`, `sprint`) — those exist only as hardcoded fallbacks inside **Spin Up Project from Template** and aren't listed anywhere.\n\n**Responses**\n- `200` — array of `{ id, name, description, category, tasks: [...], createdAt }`"
					},
					"response": []
				},
				{
					"name": "Create Project Template",
					"event": [
						{
							"listen": "test",
							"script": {
								"type": "text/javascript",
								"exec": [
									"if (pm.response.code === 201) {",
									"    const jsonData = pm.response.json();",
									"    pm.collectionVariables.set('templateId', jsonData.id);",
									"    console.log('templateId set to', jsonData.id);",
									"}",
									"pm.test('Template created', function () {",
									"    pm.response.to.have.status(201);",
									"});"
								]
							}
						}
					],
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"name\": \"New Client Onboarding\",\n    \"description\": \"Standard client onboarding checklist\",\n    \"category\": \"Client Work\",\n    \"tasks\": [\n        { \"title\": \"Kickoff Call\", \"priority\": \"high\", \"urgent\": true, \"important\": true },\n        { \"title\": \"Setup Workspace\", \"priority\": \"medium\", \"urgent\": false, \"important\": true }\n    ]\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/templates",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"templates"
							]
						},
						"description": "Saves a reusable task-list snapshot owned by the caller. Only `title`, `description`, `priority`, `urgent`, `important`, and `subtasks` are kept per task — any other fields on the input `tasks` are dropped. Sets `{{templateId}}` for use in **Spin Up Project from Template**.\n\n**Responses**\n- `201` — the created template\n- `400` — missing `name`"
					},
					"response": []
				},
				{
					"name": "Spin Up Project from Template",
					"request": {
						"method": "POST",
						"header": [
							{ "key": "Content-Type", "value": "application/json" }
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"projectName\": \"Acme Corp Onboarding\",\n    \"projectDescription\": \"Onboarding project for Acme Corp\"\n}",
							"options": {
								"raw": {
									"language": "json"
								}
							}
						},
						"url": {
							"raw": "{{baseUrl}}/api/projects/from-template/:templateId",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"projects",
								"from-template",
								":templateId"
							],
							"variable": [
								{
									"key": "templateId",
									"value": "{{templateId}}"
								}
							]
						},
						"description": "Creates a brand-new project (caller becomes owner + member) and instantiates every task from the template. `:templateId` accepts either a real template UUID you own (as used here via `{{templateId}}`) **or** one of two hardcoded string IDs that need no prior setup: `onboarding` (3-task client-onboarding flow) or `sprint` (4-task agile sprint plan). `projectName`/`projectDescription` override the template's own name/description if provided.\n\n**Responses**\n- `201` — the new project with its instantiated `tasks`\n- `404` — `:templateId` is neither one of your own template IDs nor `onboarding`/`sprint`"
					},
					"response": []
				},
				{
					"name": "Delete Template",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/templates/:id",
							"host": [
								"{{baseUrl}}"
							],
							"path": [
								"api",
								"templates",
								":id"
							],
							"variable": [
								{
									"key": "id",
									"value": "{{templateId}}"
								}
							]
						},
						"description": "Permanently deletes a template you own. Silently succeeds even if the ID doesn't exist or belongs to someone else — the query is scoped by `owner_id` but the endpoint doesn't distinguish \"not found\" from \"not yours\" in its response. **Run this last** in the folder.\n\n**Responses**\n- `200` — `{ \"message\": \"Template deleted.\" }` (always)"
					},
					"response": []
				}
			],
			"description": "Reusable task-list templates, owner-scoped, plus the endpoint that turns a template into a live project."
		},
		{
			"name": "Notifications",
			"item": [
				{
					"name": "Get My Notifications",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/notifications",
							"host": ["{{baseUrl}}"],
							"path": ["api", "notifications"]
						},
						"description": "Returns the caller's 30 most recent notifications plus their current unread count. Notifications are created automatically by the server on: being added to a project (`project_invited`), being assigned a task on create or reassignment (`task_assigned`), a task you created being marked done by someone else (`task_completed`), and every task in a project reaching `done` (`project_completed`, sent to the owner + all members) — there's no manual \"create notification\" endpoint. The actor is never notified about their own action (self-notifications are suppressed server-side).\n\n`project_completed` is guarded against re-notify spam via an internal `completed_notified_at` marker on the project: toggling a task done/undone/done again only fires once per genuine completion, not once per save. It fires from single-task update, task create, task delete, and bulk operations alike — completing a project via any of those paths triggers it.\n\n**⚠️ Known gap**: bulk status changes via **Bulk Task Operations** (Tasks folder) correctly trigger `project_completed` if they finish the project, but still don't trigger the *per-task* `task_assigned`/`task_completed` notifications that single-task updates do.\n\n**Responses**\n- `200` — `{ unreadCount, notifications: [{ id, type, title, body, entityType, entityId, actorName, isRead, createdAt }] }` (empty array if none)"
					},
					"response": [
						{
							"name": "200 OK",
							"originalRequest": {
								"method": "GET",
								"header": [],
								"url": {
									"raw": "{{baseUrl}}/api/notifications",
									"host": ["{{baseUrl}}"],
									"path": ["api", "notifications"]
								}
							},
							"status": "OK",
							"code": 200,
							"_postman_previewlanguage": "json",
							"header": [
								{ "key": "Content-Type", "value": "application/json; charset=utf-8" }
							],
							"cookie": [],
							"body": "{\n    \"unreadCount\": 2,\n    \"notifications\": [\n        {\n            \"id\": \"notif-1788173804329-czvz1\",\n            \"type\": \"task_assigned\",\n            \"title\": \"New Task Assigned\",\n            \"body\": \"You were assigned task \\\"Do the work\\\" in project \\\"Alpha Project\\\".\",\n            \"entityType\": \"project\",\n            \"entityId\": \"768ac46d-96c7-4c3f-933b-20d625af7d99\",\n            \"actorName\": \"Alice Admin\",\n            \"isRead\": false,\n            \"createdAt\": \"2026-08-31T10:56:44.329Z\"\n        },\n        {\n            \"id\": \"notif-1788173804230-nyo5j\",\n            \"type\": \"project_invited\",\n            \"title\": \"Added to Project\",\n            \"body\": \"You were added as a collaborator to project \\\"Alpha Project\\\".\",\n            \"entityType\": \"project\",\n            \"entityId\": \"768ac46d-96c7-4c3f-933b-20d625af7d99\",\n            \"actorName\": \"Alice Admin\",\n            \"isRead\": false,\n            \"createdAt\": \"2026-08-31T10:56:44.230Z\"\n        }\n    ]\n}"
						}
					]
				},
				{
					"name": "Mark Notification As Read",
					"request": {
						"method": "PUT",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/notifications/:id/read",
							"host": ["{{baseUrl}}"],
							"path": ["api", "notifications", ":id", "read"],
							"variable": [
								{ "key": "id", "value": "INSERT_NOTIFICATION_ID_HERE" }
							]
						},
						"description": "Marks one notification read. Scoped by `user_id` under the hood, so you can only ever mark your own notifications read — passing another user's notification ID silently affects zero rows rather than erroring, since the query just won't match anything.\n\n**Responses**\n- `200` — `{ \"message\": \"Notification marked as read.\" }` (always, even if the ID didn't match one of yours)"
					},
					"response": []
				},
				{
					"name": "Mark All Notifications As Read",
					"request": {
						"method": "PUT",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/notifications/read-all",
							"host": ["{{baseUrl}}"],
							"path": ["api", "notifications", "read-all"]
						},
						"description": "Marks every one of the caller's notifications read in one call — what the notification bell's \"Mark all read\" button uses.\n\n**Responses**\n- `200` — `{ \"message\": \"All notifications marked as read.\" }` (always)"
					},
					"response": []
				}
			],
			"description": "In-app notification feed — server-generated on task assignment, task completion, project completion, and project invitations, polled by the frontend bell every 15s. No endpoint here creates a notification directly; they're all side effects of actions in the Projects/Tasks folders."
		},
		{
			"name": "Analytics & KPIs",
			"item": [
				{
					"name": "Get KPI Dashboard",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{baseUrl}}/api/kpi/dashboard",
							"host": ["{{baseUrl}}"],
							"path": ["api", "kpi", "dashboard"]
						},
						"description": "Aggregate KPIs powering the Analytics page: overall completion rate, active project count, total time logged, overdue task count, a per-project health breakdown (`on-track` / `at-risk` / `delayed`), and a per-team-member workload breakdown.\n\n**Genuinely org-wide**: the query is `SELECT * FROM projects` with no visibility filter, so every project in the system counts toward these numbers for every caller, matching the \"Executive KPI & Analytics Dashboard\" / \"real-time org-wide KPIs\" framing in the frontend. Verified: an admin and an employee who shares only 1 of the admin's 2 projects both now get `totalProjects: 2` from this endpoint.\n\n**⚠️ Known consequence of being org-wide**: `projectHealthList` includes every project's actual name, and `memberWorkloadList` includes every assignee's display name next to their task/time totals - not just counts. Any authenticated user calling this endpoint can now see the name and workload of every project and every person in the company, including ones they share no project with. Confirmed live: an employee's response included another user's private project by name and that user's exact task/time totals. If per-person/per-project anonymity matters more than a literal org-wide view, this needs a follow-up (e.g. strip names from `memberWorkloadList`/`projectHealthList`, or restrict which fields non-admins receive).\n\n**Responses**\n- `200` — `{ totalProjects, totalTasks, completedTasks, overdueTasks, completionRate, totalTimeLoggedSeconds, projectHealthList: [{ id, name, totalTasks, completedTasks, overdueTasks, completionPct, health }], memberWorkloadList: [{ userId, displayName, totalTasks, pendingTasks, completedTasks, overdueTasks, timeLoggedSeconds }] }` — all fields zero/empty if you have no visible projects"
					},
					"response": []
				}
			],
			"description": "Aggregate/executive-level metrics, genuinely org-wide (every project counts for every caller) — see the request above for the naming/privacy trade-off that comes with that."
		}
	],
	"event": [
		{
			"listen": "prerequest",
			"script": {
				"type": "text/javascript",
				"exec": [
					""
				]
			}
		},
		{
			"listen": "test",
			"script": {
				"type": "text/javascript",
				"exec": [
					""
				]
			}
		}
	],
	"variable": [
		{
			"key": "baseUrl",
			"value": "http://localhost:3005",
			"type": "string"
		},
		{
			"key": "managerUserId",
			"value": "",
			"type": "string"
		},
		{
			"key": "employeeUserId",
			"value": "",
			"type": "string"
		},
		{
			"key": "projectId",
			"value": "",
			"type": "string"
		},
		{
			"key": "taskId",
			"value": "",
			"type": "string"
		},
		{
			"key": "templateId",
			"value": "",
			"type": "string"
		}
	]
}
